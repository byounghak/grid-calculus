# Validation: Phase 12 — Cahn–Hilliard example end-to-end

## Build

- [ ] CI green on Ubuntu GCC and Ubuntu Clang (Apple Clang + MSVC remain Phase 21).
- [ ] CI's `docs-build` and `docs-lint` jobs are green (no new public symbols ⇒ `\since` lint passes by default; Doxygen `WARN_IF_UNDOCUMENTED` is unaffected).
- [ ] Phase 9's FD–FFT cross-check (`fd_fft_crosscheck_test`) still passes — Phase 12 adds no new FD operators, so the fixture is untouched.
- [ ] `GRIDCALC_BUILD_EXAMPLES=OFF` (default) is a no-op for users with no `examples/` interest; configure-time succeeds without warnings.
- [ ] `GRIDCALC_BUILD_EXAMPLES=ON` builds `cahn_hilliard` cleanly under `-Wall -Wextra -Wpedantic -Wconversion`.
- [ ] `clang-format` and `clang-tidy` clean across all new files.

## Tests

- [ ] `RhsClosedFormOnSinusoid` — `computeCahnHilliardRhs` matches the analytic RHS for `ψ = sin(x)sin(y)sin(z)` within stencil-order tolerance on `N = 32`.
- [ ] `LyapunovDecay` — F(t) is monotonically non-increasing across 6 snapshots within tolerance, on a small CH simulation (`N = 32`, ~3000 RK4 steps).
- [ ] `MonotoneCoarsening` — gradient energy `∫|∇ψ|²` decreases across post-transient snapshots in the same run, satisfying the **roadmap acceptance bar** ("coarsening dynamics qualitatively match published CH benchmarks").
- [ ] `MassConservation` — `|⟨ψ⟩(t_final) − ⟨ψ⟩(0)| < 1e-10` in the same run.
- [ ] `SeedReproducibility` — fixed-seed IC is bit-reproducible.
- [ ] `vtk_writer_test` — header-shape, payload-equality, and field-name round-trip checks pass.
- [ ] All previously-passing tests remain green (no regressions): 214 tests on `clang-debug`, 138 tests on `clang-debug-nofft`. Phase 12 adds 6+ new tests in `test/cahn_hilliard_test.cpp` plus the VTK writer round-trips.
- [ ] Acceptance test wall-clock budget: `cahn_hilliard_test` completes in under 60 s on Ubuntu Clang.

## Documentation

- [ ] **No new public symbols in `include/gridcalc/`.** Phase 12 consumes Phases 1–8 and adds only `examples/` files and `test/` files; the `\since` lint regex (Phase 10) covers `include/gridcalc/` only and therefore has nothing to verify.
- [ ] User Guide chapter 12 (`docs/user-guide/chapters/12-cahn-hilliard.tex`) replaces the placeholder. Sections: motivation, free-energy form + variational derivative, discretization + stability budget, code walkthrough with `\inputminted`-style file references, running the demo, visual results (3 committed PNG snapshots), where to go next.
- [ ] Developer Note chapter 11 (`docs/developer-note/chapters/11-cahn-hilliard.tex`) follows the five-section structure (**Theory · Math derivation · Algorithm · Design decisions · References**) with the References section non-empty (≥ 1 external citation: Cahn–Hilliard 1958, Bray 1994, Provatas–Elder 2010, LeVeque 2007 RK4-stability text are all candidates).
- [ ] Three coarsening snapshots committed under `docs/user-guide/figures/cahn-hilliard/` (`early.png`, `mid.png`, `late.png`), generated by `scripts/render_ch_snapshots.py` from a documented run-recipe in the chapter.
- [ ] Both LaTeX skeletons rebuild cleanly via `scripts/build-docs.sh` (User Guide + Developer Note PDFs).
- [ ] `CHANGELOG.md` has a new `## 0.12.0 — Phase 12` block following the established format.

## Performance

- [ ] No benchmark commitments at this phase. Phase 20 will profile the explicit-RK4 hot path; the demo's small-grid wall-clock is reported in the User Guide chapter as informative-only.

## Bookkeeping

- [ ] `project(... VERSION 0.12.0)` in root `CMakeLists.txt`.
- [ ] `specs/STATUS.md` updated: Phase 12 row → Done, "Last updated" date bumped, "Next action" rewritten for Phase 13, new decisions added to "Decisions worth knowing", PR list extended.
- [ ] PR opened, every CI check green, no acceptance-bar relaxation.
